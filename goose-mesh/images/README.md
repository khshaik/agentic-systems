Place the workshop images here using the filenames below so the top-level README can reference them:

- `goose-core-architecture.png` — Core Rust codebase / interfaces / features diagram
- `goose-subagents-flow.png` — Main agent delegating to subagents, models and tools
- `goose-subagent-architecture.png` — Sub-agent token/role architecture (researcher, writer, analyst)

If you have the images attached to the repository, move or copy them into this folder using those names. The top-level `README.md` includes markdown references that will display these images once they are present.
